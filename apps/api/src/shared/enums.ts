/**
 * Role enum - defines user roles for authorization
 * Previously imported from @prisma/client, now local
 */
export enum Role {
    OWNER = 'OWNER',
    ADMIN = 'ADMIN',
    MANAGER = 'MANAGER',
    EMPLOYEE = 'EMPLOYEE',
    FINANCE_READONLY = 'FINANCE_READONLY'
}
